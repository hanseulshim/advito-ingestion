{
	"name": "validator",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"stage": "sls deploy -s stage --aws-profile advito",
		"dev": "sls deploy -s dev --aws-profile advito",
		"prod": "sls deploy -s prod --aws-profile advito"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"serverless-python-requirements": "^5.1.0"
	}
}

{
	"name": "validator",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"deploy:stage": "sls deploy -s stage --aws-profile advito",
		"deploy:dev": "sls deploy -s dev --aws-profile advito",
		"deploy:prod": "sls deploy -s prod --aws-profile advito",
		"deploy:all": "yarn deploy:dev && yarn deploy:stage && yarn deploy:production"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"serverless-python-requirements": "^5.1.0"
	}
}
